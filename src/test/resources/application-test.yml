spring:
  datasource:
    url: jdbc:tc:mysql:8.4:///pms?TC_DAEMON=true&allowPublicKeyRetrieval=true&useSSL=false
    username: test
    password: test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    enabled: true
