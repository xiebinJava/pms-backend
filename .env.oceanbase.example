# Copy to .env.oceanbase.local and keep the local file out of Git.
OCEANBASE_HOST=127.0.0.1
OCEANBASE_PORT=2881
OCEANBASE_DATABASE=brad_pms
OCEANBASE_ROOT_PASSWORD=replace_with_root_password
PMS_APP_PASSWORD=replace_with_pms_app_password
PMS_MIGRATOR_PASSWORD=replace_with_pms_migrator_password

# Direct backend start uses the least-privileged account.
OCEANBASE_USER=pms_app
OCEANBASE_PASSWORD=replace_with_pms_app_password

# At least 32 bytes. Generate with: openssl rand -hex 32
PMS_JWT_SECRET=replace_with_a_random_secret_at_least_32_bytes
PMS_CORS_ALLOWED_ORIGINS=http://localhost:57979,http://127.0.0.1:57979
PMS_MANAGEMENT_ADDRESS=127.0.0.1
PMS_MANAGEMENT_PORT=8081
PMS_BOOTSTRAP_ADMIN_EMAIL=brad.xie@example.com
# Optional display names; legacy username is retained only for old clients.
PMS_BOOTSTRAP_ADMIN_NAME_ZH=谢斌
PMS_BOOTSTRAP_ADMIN_USERNAME=brad.xie
PMS_BOOTSTRAP_ADMIN_PASSWORD=replace_with_bootstrap_password_12_chars
PMS_DEPLOYMENT_ENV=development

# Local Compose defaults (production must override these values).
PMS_NOTIFICATION_STARTUP_CHECK=false
PMS_PASSWORD_RESET_EXPOSE_TOKEN=true
PMS_INVITATION_EXPOSE_TOKEN=true

# Production notification settings: set startup check=true, token exposure=false,
# configure SMTP credentials, and use an https:// PMS_PUBLIC_BASE_URL.
