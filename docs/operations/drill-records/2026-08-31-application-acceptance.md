# 应用目标环境验收记录（2026-08-31）

> 历史记录说明：本文保留当日应用验收快照（当时健康检查显示 migration=7）。随后数据库基线已推进到 V10；当前版本、测试数量和迁移状态以 [`infrastructure-status.md`](../infrastructure-status.md) 为准。

## 结果

本机已启动前端 `127.0.0.1:57979` 和后端 `127.0.0.1:8080`，连接真实 MySQL `pms` 完成非生产浏览器验收。测试账号通过运行时环境注入，密码不写入本记录。

## 已核对

- 前端 CI `33351102816` 已通过测试、类型检查、生产构建、Nginx 镜像 Trivy 和 SPDX SBOM。
- 后端 `/api/health/live` 返回 200；`/api/health/ready` 返回 200，数据库状态为 `UP`，迁移版本为 `7`。
- `E2E_BASE_URL=http://127.0.0.1:57979 pnpm exec playwright test`：桌面流程和 390px 窄屏流程共 2 项通过（最终运行 6.7 秒）。
- 覆盖邮箱登录、项目列表与详情入口、项目成员展示和窄屏布局；浏览器依赖首次缺失后已安装 Chromium 并重新执行通过。
- 收口复核时 Playwright Chromium 在宿主机启动阶段偶发 `SIGTRAP/SIGABRT`，未进入页面断言；使用同一地址的应用内浏览器重新核对桌面登录页和 390px 窄屏登录页，页面无错误日志且无横向溢出。该现象属于宿主浏览器进程稳定性，不改变此前 2 项通过结果。

## 待执行验收路径

在具备 Docker 镜像缓存、生产密钥和可用 MySQL 客户端的目标机仍需复验：

1. 按升级手册启动 Compose，确认 `/api/health/live`、`/api/health/ready` 和前端健康检查。
2. 使用邮箱（大小写混合）登录，覆盖项目列表/详情、组织架构、人员权限、角色数据范围和导入预览/提交；另验证旧英文名登录兼容。
3. 验证项目经理、业务线层级、节点负责人/排期、任务删除和里程碑操作与 API 数据一致。
4. 用 390px 窄屏和桌面宽度各执行一次 Playwright 冒烟，保存截图与报告。
5. 验收结束注销并确认旧 token 失效、审计日志写入 request id。

本记录证明开发环境的真实前后端链路和浏览器冒烟已通过；不代表目标企业生产环境验收完成。生产环境必须使用企业自己的测试账号和密钥，禁止复用本机凭据。
