# 应用目标环境验收记录（2026-08-31）

## 结果

本机当前未运行前后端服务，且前端依赖目录在磁盘空间不足时已清理；本次不伪造浏览器通过结果，目标环境验收保持阻塞。

## 已核对

- 前端 CI `33351102816` 已通过测试、类型检查、生产构建、Nginx 镜像 Trivy 和 SPDX SBOM。
- 本地请求 `127.0.0.1:57979` 与 `127.0.0.1:8080` 当前均无法建立连接（服务未启动）。
- 前端仓库 `dist/` 仍存在，但静态文件服务不包含 `/api` 反向代理，不能代替完整验收。

## 待执行验收路径

在具备 Docker 镜像缓存、生产密钥和可用 OceanBase 客户端的目标机：

1. 按升级手册启动 Compose，确认 `/api/health/live`、`/api/health/ready` 和前端健康检查。
2. 使用邮箱（大小写混合）登录，覆盖项目列表/详情、组织架构、人员权限、角色数据范围和导入预览/提交；另验证旧英文名登录兼容。
3. 验证项目经理、业务线层级、节点负责人/排期、任务删除和里程碑操作与 API 数据一致。
4. 用 390px 窄屏和桌面宽度各执行一次 Playwright 冒烟，保存截图与报告。
5. 验收结束注销并确认旧 token 失效、审计日志写入 request id。

本记录仅表示验收入口和阻塞事实；在目标服务启动并完成上述步骤前，发布清单中的浏览器验收项目保持未勾选。
