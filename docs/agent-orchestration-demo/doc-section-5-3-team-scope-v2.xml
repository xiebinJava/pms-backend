<p>看板采用“计划审查—运行—门禁—汇报”的连续视图，并绑定当前业务团队：执行前展示可编辑的角色阵容、任务依赖和预算，用户确认后才创建成员会话；运行中展示 ready/running/blocked/completed 状态、attemptId、检查点和人工接管入口；停止或刷新后从持久化状态恢复，不重复已确认的业务副作用。团队内路由统一为 <code>/teams/:teamKey/plans</code>、<code>/teams/:teamKey/runs</code>；全局运行中心只做聚合，不直接改变业务状态。</p>
