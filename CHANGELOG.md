# 变更记录

## 未发布

- 运行时、本地 Compose、Helm 和测试都只使用 MySQL 8；Flyway 随后端启动，测试使用 Testcontainers。
- 长 JSON 与方案字段改为 MySQL 可用的 `TEXT` / `MEDIUMTEXT`，以便在 MySQL 8.4 `utf8mb4` 上执行 V1–V41。
- 将 `orgUnitId` 映射到 `org_unit_id`，避免组织更新查询不存在的 `orgunitid` 列。
- 首次安装指引指向公开的 `pms-distribution` 仓库，并使用公开发布的 GitHub 克隆地址。
- 发布收口快照（2026-09-01）：`mvn -q test` 187 项通过（0 失败、0 错误、1 跳过）；就绪检查返回 `migration=10`；V1–V10 备份恢复与关键表行数比对通过。
- 就绪检查对齐 V10 迁移基线；备份元数据按数字排序迁移版本，并记录每张表的精确行数。
- 新增 `GET /workbench`，首页一次加载我的任务、参与项目和最近评论。
- 任务详情支持子任务、任务评论以及图片 / PDF 附件。
- 任务指派和评论写入站内通知，并提供跨项目、任务、里程碑和评论的全局搜索。
- 增加贡献者 MySQL 8 启动路径（`docker-compose.mysql.yml` + Flyway），数据库为 `pms`。
- 增加可选的 OIDC / LDAP 登录。默认仍是本地邮箱登录；SSO 只允许已邀请账号登录。
- 附件支持可选的 S3 兼容存储（MinIO 或云存储）。默认仍是本地上传卷。
- 任务指派和评论可选择签名 Webhook。投递失败不会回滚站内通知。
- 项目评论会通知关注人；节点完成或回退时写入收件箱事件。
- 在私有管理端口暴露 `/actuator/prometheus`，并提供可选的本机 Prometheus / Grafana Compose 叠加层。
- 增加后端 + 前端 Helm chart。Chart 不安装 MySQL；ServiceMonitor 和 Ingress 默认关闭。

## 2026-09-02

- 生产就绪第一阶段本地核验：生产配置门禁测试通过；数据库 V1–V10 幂等、备份校验和、隔离恢复及拒绝边界通过；冒烟测试改为邮箱优先登录并保留用户名兼容；后端当时 190 项测试（0 失败、0 错误、1 跳过），前端 112/112，本机桌面 / 移动 Playwright 验收通过。企业生产凭据和基础设施仍由部署方提供。

## 1.0.0

- 单企业自部署后端，运行在 MySQL 8。
- 邮箱身份、RBAC、组织树、项目生命周期和审计日志。
- CI 增加隐私黑名单扫描。演示账号只使用虚构身份。
